include_directories(../nfs/include)
include_directories(include)

file(GLOB_RECURSE <PROJECT_NAME>_SRC
	"include/*.h"
	"src/*.c"
	"include/*.hpp"
	"src/*.cpp"
)

add_executable(
	<PROJECT_NAME>
	${<PROJECT_NAME>_SRC}
)

target_link_libraries(<PROJECT_NAME> nfs)